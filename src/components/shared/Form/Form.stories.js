import React from "react";
import Form from ".";
export default {
    component: Form,
    title: 'Form',
};

const Template = props => <Form {...props} />;

export const Default = Template.bind({});





